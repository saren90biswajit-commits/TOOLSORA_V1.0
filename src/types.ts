export interface ToolCardData {
  id: string;
  name: string;
  description: string;
  icon: string;
  isFree: boolean;
  href: string;
}

export type Platform = 'Instagram' | 'TikTok' | 'YouTube' | 'Facebook' | 'LinkedIn' | 'X';
